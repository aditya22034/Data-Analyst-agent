To run the code, follow these steps: First, install the necessary dependencies by running the following command in the terminal: "pip install streamlit pandas matplotlib seaborn pymupdf python-docx pytesseract google-generativeai pillow"  . After installing the dependencies, navigate to the project folder and start the Streamlit application using  "streamlit run app.py" . This will open a web interface where users can upload files (.doc, .txt, .csv, .xlsx, .pdf, images), ask questions, and generate data visualizations. The application integrates Google Gemini API to process queries and provide insights.
And please insert your own gemini api key in this 
genai.configure(api_key="YOUR_GEMINI_API_KEY").
